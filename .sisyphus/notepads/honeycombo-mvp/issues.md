- GitHub repo creation was blocked because gh is not installed and no GitHub auth token/remote is available in this environment.

- PowerShell `Set-Content -Encoding utf8` wrote a BOM during invalid-file testing, which triggered a JSON parse error before schema validation.
